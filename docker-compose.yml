version: "3"

services:
  proxy:
    build:
      context: .
      dockerfile: docker/Dockerfile
    environment:
      HOST: "${HOST}"
    ports:
      # proxy
      - 9090:9090
      # admin site
      - 9091:9091

